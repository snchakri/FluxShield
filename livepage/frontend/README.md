
  # Live Traffic Monitoring Page

  This is a code bundle for Live Traffic Monitoring Page.
  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  